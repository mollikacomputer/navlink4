export const metadata = {
    title: 'Dashboard || Ranjit',
    description: 'Dashboard Description here',
  }
const layout = ({children}) => {
    return (
        <div>
            {children}
        </div>
    );
};

export default layout;