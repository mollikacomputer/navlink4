export const metadata = {
    title: 'Contact||Ranjit',
    description: 'Generate meta data description contact page',
  }

const layout = ({children}) => {
    return (
        <div>
            {children}
        </div>
    );
};

export default layout;