export const generateMetadata = () =>{
    return {
        title:"Services||Ranjit",
        description:"Services page generate meta data ",
    }
};

const layout = ({children}) => {
    return (
        <div>
            {children}
        </div>
    );
};

export default layout;